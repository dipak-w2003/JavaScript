# ⚡ Advanced React – 7-Day Revision Summary (High-Level Only)

1. Rendering & Fiber
   - Reconciliation
   - React Fiber internals
   - Render phase vs Commit phase
   - StrictMode double-invocation

 <br>

2. Hooks
   - `useState` batching
   - `useEffect` triggers & cleanup
   - `useLayoutEffect` vs `useEffect`
   - `useRef` (stable identity)
   - Custom hooks architecture

<br>

3. Performance
   - `React.memo`
   - `useCallback` / `useMemo`
   - `Suspense` (lazy loading)
   - Avoiding unnecessary re-renders
   - Virtualized lists (`react-window`)

<br>

4. State Management
   - Context (avoid for large scaling)
   - `useReducer` for complex logic
   - Redux Toolkit
   - RTK Query for data fetching
   - Slices + async thunks

<br>

5. Routing
   - Nested routes
   - Protected routes
   - Loader-based data fetching
   - Code splitting with `lazy()`

<br>

6. Architecture
   - Folder structure (feature-based)
   - Component composition
   - Presentational vs Container components
   - Smart vs Dumb components
   - Reusable UI patterns

<br>

7. Production-Level Skills
   - Error boundaries
   - Handling race conditions
   - Optimizing images (`CDN`, `lazy`, `prefetch`)
   - Accessibility (aria props)
   - Performance profiling (React DevTools)
